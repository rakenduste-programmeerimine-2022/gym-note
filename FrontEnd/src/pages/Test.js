import { Container, Box, Grid } from "@mui/material";

export default function Test(){
                <Box sx={{bgcolor: '#9b9b9b', height: '120vh'}} style={{ marginTop: "5vh" }}>
                    <Box textAlign={"left"} lineHeight={20} paddingLeft={3} color={"white"} fontSize={30}>
                        TEST SITE</Box>
                    <Grid container spacing={0.5} paddingX={8} paddingTop={50}>
                    </Grid>
                </Box>
}